syntax = "proto3";

package schemas;

option go_package = "github.com/mickep76/osquery-protobuf/pkg;schemas";

// ELF dynamic section information.
message ElfDynamic {
  // Tag ID
  int32 tag = 1;
  // Tag value
  int32 value = 2;
  // Class (32 or 64)
  int32 class = 3;
  // Path to ELF file
  string path = 4;
}
